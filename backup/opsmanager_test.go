package backup_test

import (
	. "github.com/onsi/ginkgo"
	. "github.com/onsi/gomega"
)

var _ = Describe("OpsManager", func() {
	Describe("", func() {
		Context("", func() {

			It("", func() {
				Ω(nil).Should(BeNil())
			})
		})
	})
})
