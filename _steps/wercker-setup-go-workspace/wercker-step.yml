name: setup-go-workspace
version: 1.2.1
description: |
  Adds the project to the Go workspace directory hierarchy.
  You can set the `package_dir` option to override the default package directory that is
  the git provider url + user + project, eq: `github.com/wercker/go-logging/logging`
